const fs = require('fs');
const d3 = require('d3-geo'); // We might not have d3-geo installed. Wait, npm install d3-geo?
// Let's just write a plain script that runs in the browser via Puppeteer, or just logs it from Map.php!
